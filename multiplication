#include <stdio.h>

int main() {
    int a, b, product;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    product = a * b;

    printf("Multiplication = %d\n", product);

    return 0;
}


OUTPUT
Enter two numbers: 2 4
Multiplication = 8
